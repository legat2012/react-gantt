/**
 * Возвращает максимальное из двух значений
 *
 * @param {any} a - Левый операнд сравнения
 * @param {any} b - Правый операнд сравнения
 * @returns {any}
 */
export const max = (a, b) => a > b ? a : b;