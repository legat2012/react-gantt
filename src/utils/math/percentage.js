/**
 * Возвращает процентное соотношение между двумя числами
 *
 * @param {number} value - Относительное значение
 * @param {number} base - Базовое значение
 * @returns {number}
 */
export const percentage = (value, base) => value / base * 100;